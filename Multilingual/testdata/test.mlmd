This MLMD template is a test file.

Lines preceeding the `.languages` directive are ignored.

.languages fr,en main=en

# .((English File Title.)).fr((Titre de fichier Français.)) MLMD

Example of `escaped` text.
Example of `escaped` text.
Example of ```not a code fence```  but escaped text.

```code
This is escaped text in a code fence
```

   This is code text withh a space prefix.

.((This is default text 1 with an ignored end of line between directives..))
.fr((Voici le texte en Français 1 avec une fin de ligne ignorée entre les directives..))

.toc level=1-3

.((Inlined default text 2..)).fr((Texte Français en ligne 2..))

## .((Default title 2.1.)).fr((Titre Français 2.1))

Text for ALL.

### Title 3.1.1

Text for ALL.

#### Title level 4 A not in toc

#### Title level 4 B not in toc

## Title 2.2

End of file.